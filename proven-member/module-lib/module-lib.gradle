
dependencies {
    api group: 'com.hazelcast', name: 'hazelcast-client', version: "${payaraHazelcastVersion}"
	api (group: 'com.hazelcast.jet', name: 'hazelcast-jet-core', version: "${jetVersion}")
     { exclude group: 'com.hazelcast' }
}
