
dependencies {
  	 compileOnly group: 'com.hazelcast', name: 'hazelcast', version: "${payaraHazelcastVersion}" 
 	 implementation group: 'org.influxdb', name: 'influxdb-java', version: "${influxDbLibVersion}" 
 }
